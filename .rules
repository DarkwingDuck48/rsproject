## О проекте
- RS Project — Open Source аналог MS Project на Rust
- Это обучающий проект: не пиши код самостоятельно, веди себя как ментор
- Это обучающий проект, однако все решения внутри по архитектуре и вариантам реализации применяются как для продуктивного кода
- Всегда объясняй наиболее важные моменты в коде, архитектуре или командах
- Следуй Best Practice при написании кода
- **Текущий релиз:** milestone v0.2.0 «Стабильность, UX и документация» — стримы 6–8 (тесты и надёжность ядра, UX и обработка ошибок, документация и примеры). Статусы задач — в GitHub issues, локальная выжимка — в `todo.md`
- **Завершённые стримы:** 5 (миграция на Tauri v2) — выпущен релиз 0.1.1
- **Главная цель стрима 5 (достигнута):** научиться использовать Tauri в проектах на Rust
- Когда я буду готов сделать релиз - править файл `./CHANGELOG.md`

## Структура проекта
- Cargo workspace из трёх крейтов: `app` (Tauri), `logic` (бизнес-логика) и `tools` (инструменты разработки)
- `./app` — десктоп-приложение на Tauri v2 + React 19 + JavaScript
  - `./src-tauri/` — Rust-бекенд (Tauri commands, state)
  - `./src/` — React/JS фронтенд (Vite)
- `./logic` — вся бизнес-логика: структуры данных и сервисы (не зависит от UI)
- `./tools` — инструменты разработки: генераторы демо-проектов (`src/bin/gen_demo_project.rs`, `gen_big_project.rs`); не входит в релизные бандлы
- `./examples` — готовые JSON-проекты для загрузки в приложении через «Файл → Открыть проект»

## Технический стек
- Rust edition 2024
- GUI: Tauri v2 + React 19 + JavaScript + Vite
  - UI-библиотека: Ant Design v6 (`antd` + `@ant-design/icons`)
  - Даты: `dayjs` (античный тип `DatePicker` в antd v6 — dayjs-объекты)
  - Tauri-плагины: `opener` (внешние ссылки), `dialog` (файловые диалоги), `shell`
  - Стилизация: CSS Grid + CSS Variables (темизация; `antd` использует `--ant-*`)
  - Имена аргументов `invoke()`: верхнеуровневые ключи — в `camelCase` (макрос `#[tauri::command]` конвертирует snake_case-параметры; напр. `date_start` → `dateStart`), а поля вложенных DTO — snake_case по именам Rust-структур (serde)
- Сериализация: serde + serde_json
- Дата/время: chrono 0.4
- Идентификаторы: uuid v4
- Обработка ошибок: anyhow, thiserror

## Архитектура `logic`
- `base_structures/` — доменные структуры:
  - `project.rs` — проект (основная сущность)
  - `tasks.rs` — задачи с иерархией
  - `resource.rs` / `resource_pool.rs` — ресурсы и пул ресурсов
  - `dependencies.rs` — зависимости между задачами (предшественники)
  - `project_calendar.rs` — календарь проекта
  - `time_window.rs` — временные окна для доступности ресурсов
  - `project_containers.rs` — контейнеры проекта
  - `traits.rs` — трейты
  - `cust_exceptions.rs` — пользовательские исключения
- `services/` — сервисный слой:
  - `scheduler.rs` — планировщик: расчёт критического пути (топологическая сортировка, ES/EF/LS/LF); даты задач не перепланирует
  - `task_service.rs` — сервис задач
  - `resource_service.rs` — сервис ресурсов (проверка доступности)

## Инструменты и примеры (`tools`, `examples`)
- `tools/src/bin/` — генераторы демо-данных: `gen_demo_project.rs`, `gen_big_project.rs`, `gen_apartment_renovation.rs`. Собирают проект через публичный API `logic` и печатают JSON в stdout
- Перегенерация (из корня репозитория): `cargo run -p tools --bin gen_apartment_renovation > examples/apartment_renovation.json`
- `examples/*.json` — демо-проекты для загрузки через «Файл → Открыть проект»: `demo_project` (IT), `big_project` (крупный IT-проект), `apartment_renovation` (ремонт квартиры); это эталонные снапшоты, а не reproducible-артефакт: при повторной генерации отличаются UUID (v4) и порядок ключей `HashMap`
- Важно: `Scheduler` считает критический путь только по длительностям и зависимостям от даты старта проекта; записанные в задачах даты и `date_end` проекта на расчёт не влияют (в `backward_pass` параметр `project_end` вообще не используется — у задач без последователей поздний финиш берётся как максимальный ранний финиш)

## Архитектура `app` (Tauri v2)
- `src-tauri/` — Rust-бекенд:
  - `src/main.rs` — точка входа Tauri
  - `src/lib.rs` — `tauri::Builder` + регистрация плагинов
  - `src/state.rs` — `AppState` с `Mutex<SingleProjectContainer>`
  - `src/commands.rs` + `src/commands/` — Tauri-команды (`#[tauri::command]`):
    - `project.rs` — создание, открытие, сохранение, закрытие проекта
    - `task.rs` — CRUD задач, зависимости, назначение ресурсов, расчёт критического пути, `get_critical_path` (чтение общего стейта)
    - `resources.rs` — CRUD ресурсов, периоды недоступности, проверка доступности
    - `utils.rs` — вспомогательные функции (`parse_date`, `resolve_project_id`)
  - `src/dto.rs` + `src/dto/` — DTO для фронтенда (сериализация из `logic`):
    - `project.rs` / `task.rs` / `resources.rs`
  - `tauri.conf.json` — конфигурация окна, бандла, идентификатор
  - `capabilities/default.json` — разрешения плагинов
- `src/` — React/JS фронтенд (`.jsx`):
  - `main.jsx` — рендер React-приложения, импорт `antd/dist/reset.css`
  - `App.jsx` — корневой компонент, стейт активной вкладки, CSS Grid-раскладка
  - `tabs.jsx` — реестр вкладок (единый источник истины для панелей)
  - `components/layout/` — TopPanel (меню Файл/Помощь + переключение вкладок + переключатель темы, 5.16, + горячие клавиши Ctrl/Cmd+N/S/O, 5.17), SidePanel (список элементов вкладки), CentralPanel (переключатель вьюх), StatusBar (сводка по проекту: задачи/ресурсы/крит. путь/сохранение, 5.18)
  - `components/views/` — ProjectView (просмотр/редактирование проекта, 5.11), TasksView (дерево задач + CRUD, 5.12), ResourcesView (таблица ресурсов + периоды недоступности, 5.13), GanttView (SVG-диаграмма: полосы задач по дням, зависимости-стрелки, подсветка критического пути, тултипы, гориз. скролл, 5.15)
  - `components/dialogs/` — 9 диалогов (5.14): About, New/EditProject, CloseProject, NewTask/TaskDetails, New/EditResource, AssignResource, UnavailablePeriod + общие `ProjectFormFields`
  - `hooks/` — кастомные React-хуки: `useTheme` (темизация, 5.16), `useHotkey` (горячие клавиши, 5.17)
  - `context/` — React-контексты: `SelectionContext` (общее выделение задач/ресурсов для SidePanel и вьюх, 5.21)
  - `types/` — JSDoc-типы, документирующие структуры `logic`
  - `lib/` — обёртки над `invoke()` для вызова Tauri-команд: `api.js` (все команды), `options.js` (лейблы enum'ов), `format.js` (даты/плюрализация/дата-время), `constants.js` (версия, ссылка на репо), `errors.js` (различение «проект не выбран» от реальных ошибок)
- `index.html` — точка входа WebView
- `package.json` — npm-зависимости
- `vite.config.js` — конфигурация Vite (сборщик фронтенда)
- Бинарник: `rsproject` (app/src-tauri/src/main.rs)

## Реализованные возможности
- Создание/редактирование проектов, задач и ресурсов (через UI: меню «Файл», вкладки «Задачи»/«Ресурсы»)
- Назначение ресурсов с проверкой доступности
- Зависимости задач (добавление/удаление в TaskDetailsDialog)
- Иерархия задач (групповые задачи)
- Расчёт критического пути
- Диаграмма Ганта с подсветкой критического пути (в 5.15)
- Темизация: светлая/тёмная тема с сохранением выбора (5.16)
- Горячие клавиши: Ctrl/Cmd+N/S/O и Delete (5.17)
- Статус-бар: задачи, ресурсы, длительность крит. пути, дата сохранения (5.18)
- Сохранение/загрузка проекта в JSON
- Адаптивный ресайзинг окна: таблицы со скроллом вместо сжатия колонок, брейкпоинты раскладки, минимальный размер окна 640×480 (5.24)

## В планах
- Открытие нескольких проектов и объединение их в один
- Импорт/экспорт в формат MS Project (обратная совместимость)

## Сборка и тесты
```bash
cargo build --release       # сборка всего workspace (Rust-часть; фронтенд НЕ собирается)
cargo build -p logic        # сборка только logic
cargo build -p app          # сборка только Tauri-бекенда
cargo test                  # запуск всех тестов (логика: logic/tests/integration.rs)
cargo fmt                   # форматирование кода
cargo clippy                # линтер
cd app && npm run dev       # запуск Vite dev-сервера (фронтенд)
cd app && npm run tauri dev # запуск полного приложения (локальный CLI v2 из devDependencies)
# cargo tauri dev           # не использовать: глобальный CLI может быть v1 и не читать tauri.conf.json v2
cd app && npm run tauri build  # production-сборка: фронтенд + бандлы → target/release/
```
Бинарник после сборки: `./target/release/rsproject`

> Рабочий бинарник требует собранного фронтенда (`app/dist`) и фичи `custom-protocol`:
> голый `cargo build --release` без них даёт бинарник, который ищет dev-сервер
> на `http://localhost:1420`. Поэтому production-сборка — только через `npm run tauri build`.

## Процесс работы по задачам
- **Источник истины по задачам — GitHub (issues + milestones).** `todo.md` — локальная выжимка по стримам: он в `.gitignore` и в репозиторий не попадает. При расхождении верным считается GitHub.
- При начале новой сессии — прочитай `todo.md` (стримы и текущие задачи) и сверься с открытыми issues текущего milestone.
- Пользователь сообщает, какую задачу он взял (например, «я делаю задачу 1.1»).
- Пользователь выполняет задачу самостоятельно, затем я делаю код-ревью.
- При проведелии код-ревью, воспользуйся /skill rust-best-practices - в этом скилле собраны лучшие практики написания кода на Rust.
- В случае работы над фронтендом, ты можешь писать код, если это визуальные изменения.
- После успешного ревью — помечаю задачу как выполненную и при необходимости ставлю следующую.
- Новые задачи заводятся issue в GitHub (с милстоуном и метками), после чего попадают в `todo.md` как задача стрима.

## Соглашения
- Версионирование: SemVer (текущая 0.1.2)
- Лицензия: Apache-2.0
- Репозиторий: https://github.com/DarkwingDuck48/rsproject
- При любом изменении архитектуры (добавление/удаление/переименование модулей, файлов, сервисов, структур) — самостоятельно актуализировать этот `.rules` файл без напоминания
